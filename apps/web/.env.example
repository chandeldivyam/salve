# In dev, leave this unset — Vite's server.proxy handles /api/** transparently
# so the browser sees same-origin requests (no CORS preamble, cookies just
# work). Set it in prod to point at the API origin, e.g.:
#   VITE_API_URL=https://api.salve.app
# VITE_API_URL=
