// @opendesk/mutators — Zero custom mutators, shared by web (optimistic) and api (authoritative).
// Phase 0 placeholder: real defineMutators({...}) implementation lands in Phase 2.

import { ZERO_SCHEMA_NAME } from '@opendesk/zero-schema';

export const MUTATORS_SCHEMA = ZERO_SCHEMA_NAME;
