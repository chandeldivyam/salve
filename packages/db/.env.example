# Drizzle-kit reads DATABASE_URL from this file (or the shell) for `db:generate`,
# `db:migrate`, `db:push`, `db:studio`. Match the value in apps/api/.env in dev.
DATABASE_URL=postgres://opendesk:opendesk@localhost:5433/opendesk
